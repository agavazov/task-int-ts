{
  "name": "test-exercise",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "test-1": "jest --no-cache -i task1",
    "test-2": "jest --no-cache -i task2",
    "test-3": "jest --no-cache -i task3",
    "test-4": "jest --no-cache -i task4",
    "test": "jest --no-cache"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "jest": {
    "testEnvironment": "node",
    "testRegex": ".*\\.spec.js$",
    "transform": {
      "^.+\\.(t|j)sx?$": [
        "ts-jest",
        {
          "diagnostics": false
        }
      ]
    }
  }
}
